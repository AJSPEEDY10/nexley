<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Nexley</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- ============================================================
	     THESE TWO ARE NOT OPTIONAL AND THEIR ABSENCE IS NOT A PROMPT.

	     Nexley photographs a page in two places — a photo attached to a note
	     (app/app.html #photoFile) and a photo of a marked paper (#pprPhotoFile),
	     both `<input type="file" accept="image/*" capture="environment">`. Inside
	     a WKWebView that opens the system camera and photo picker, and iOS
	     TERMINATES an app that reaches either one without the matching usage
	     string here. Not a denied permission — a crash, the first time a student
	     taps "Take a photo". There were no usage strings in this file at all,
	     which means every camera tap in the iOS build would have killed the app.
	     Found 2026-09-09 from the "justify every permission the native wrap
	     requests" line on the App Store checklist in GROWTH_AND_LAUNCH.md §11.

	     The wording matters as much as the presence. Apple rejects vague strings,
	     and the reviewer reads these — so each one says the specific thing the
	     app does with it, not "we need access to your camera".

	     Nothing else is requested: no microphone, no location, no contacts, no
	     photo-library WRITE (nothing is ever saved back to the camera roll), and
	     notifications need no key because iOS asks at runtime.
	     ============================================================ -->
	<key>NSCameraUsageDescription</key>
	<string>So you can photograph a page of a textbook, the board, or a marked paper and attach it to the note or result it belongs to. Nexley only opens the camera when you tap the photo button.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>So you can attach a photo you have already taken — a page, the board, or a marked paper — to a note or a result. Nexley never browses your library on its own; you pick the photo.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
