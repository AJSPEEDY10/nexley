<?xml version="1.0" encoding="utf-8"?>
<!-- Nexley — home-screen widget layout.
     Plain RemoteViews-compatible views only (no ConstraintLayout, no custom
     views) — AppWidgets render in a separate process and only support a
     documented subset of layouts/views. Unverified on a real device, see
     DueWidgetProvider.kt's own header. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:padding="16dp"
    android:background="#1E4D3E">

    <TextView
        android:id="@+id/widget_headline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nexley"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_detail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="#D8E5DF"
        android:textSize="13sp" />

</LinearLayout>
