{
  "name": "nexley",
  "private": true,
  "version": "0.19.1",
  "description": "Nexley native wrap (Capacitor). The web app itself has no build step; this only packages app/ for the App Store / Play Store.",
  "scripts": {
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/cli": "8.5.1",
    "@capacitor/core": "8.5.1",
    "@capacitor/ios": "8.5.1"
  }
}
